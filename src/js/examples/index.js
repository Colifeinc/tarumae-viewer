
// import Tarumae from "../tarumae"

window.addEventListener("load", function() {

	// var renderer = new Tarumae.Renderer();

	// var scene = renderer.createScene();

	// window._scene = scene;
 
	// scene.createObjectFromURL(cube_mod, obj => {
	//   scene.add(obj);
	// });
	//
	// var modelViewer = new Tarumae.ModelViewer(scene);
	// modelViewer.enableDragAcceleration = true;


	// const g = renderer.drawingContext2D;

	// var vertices = [new Vec2(100, 100), new Vec2(200, 100),
	//   new Vec2(200, 200), new Vec2(100, 200)];
  
	// var edges = [{ from: vertices[0], to: vertices[1] },
	// { from: vertices[0], to: vertices[1] }]

	// function drawVertices() {
	//   for (var i = 0; i < vertices.length; i++) {
	//     g.drawPoint(vertices[i], 9);
	//   }
	// }

	// function detectArea() {
	//   var v1 = vertices[0];


	// }
  
	// scene.onframe = function(r) {
    
	//   drawVertices();
    
	// };

	// scene.show();
});